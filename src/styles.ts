import * as b from "bobril";
import * as Font from "bobwai--font";

export const chatSidebarHeaderUserInfoStyle = b.styleDef({
    padding: "8px",
    lineHeight: "101px",
});

export const chatSidebarHeaderWrapperStyle = b.styleDef({
    padding: "8px",
});
